Create Plugins here:

Working Plugin: DiffDrive.cpp/hpp

`mkdir build`
`cd build`
`cmake ..`
`make`

Make sure to source the path to build file.
Can put in ~/.bashrc:
`export IGN_GAZEBO_SYSTEM_PLUGIN_PATH="<PATH>"`